<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:id="@+id/section_title"
        style="@style/BaseTextView.SectionTitle"
        tools:text="Datos del Propietario" />

    <LinearLayout
        android:id="@+id/section_items_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingLeft="@dimen/small_space"
        android:paddingRight="@dimen/small_space" />
    <Button
        android:id="@+id/section_items_add_other"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_shape"
        android:textColor="#FFF"
        android:text="@string/add_other_section"
        android:visibility="visible"
        android:textSize="20dp"
        android:layout_gravity="center"
        android:gravity="center"/>

</LinearLayout>